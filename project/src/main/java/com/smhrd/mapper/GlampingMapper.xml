<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.mapper.GlampingMapper">
	<select id="my_glamping_show" parameterType="String" resultType="GlampingDTO">
	select * from glamping_caravan_tb where gc_num in(select gc_num from room_tb where room_num in(select room_num from reservation_calendar_tb
	where mb_email = #{mb_email}))
	</select>
</mapper>